<div class="p-8">
  <h1 class="font-bold text-4xl mb-4">Reviews</h1>
  <% @reviews.each do |r| %>
    <p class="text-gray-600"><%= r.company_name %></p>
  <% end %>
  <p class="text-gray-600">Total reviews: <%= @reviews.count %></p>
</div>
<div class="mt-4">
  <%== pagy_nav(@pagy) %>
</div>